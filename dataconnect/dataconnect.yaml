specVersion: "v1beta"
serviceId: "SharedShoppingList"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "SharedShoppingList-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
